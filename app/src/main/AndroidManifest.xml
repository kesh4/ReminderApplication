<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mydev.reminderapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReminderApplication">
        <activity android:name=".AddEvent2" />
        <activity android:name=".AddEvent1" />
        <activity android:name=".NotificationClicked" />
        <activity android:name=".MainActivity" />

        <receiver android:name=".BackgroudTimerService" />
    </application>

</manifest>